#!/usr/bin/env python3

import sys

from src.user import checkOptions
from src.files import *
from src.extract import *

# --- Check args ---

checkOptions(sys.argv[1:])


# --- Retrieves all project files --- 

files = getFiles(".kt")

# Print the list of files

# for file in files:
#     print(f"{file}")

print(files[6])

comments, functions = extractData(files[6])

if len(comments) == len(functions):
    for i in range(len(comments)):
        function = extractComment(comments[i])
        extractFunction(functions[i])

        #print(function)